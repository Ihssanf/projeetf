spring.application.name=GestionReservations-SallesReunion
server.port=8085


spring.datasource.url = jdbc:mysql://localhost:3306/jmrs?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC

# Username and password
spring.datasource.username = root
spring.datasource.password =

# ===============================
# = JPA / HIBERNATE
# ===============================

# Show or not log for each SQL query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated according to the Java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Optionally, you can set the dialect for MySQL
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect
spring.main.allow-bean-definition-overriding=true
@SpringBootApplication(exclude = WebMvcAutoConfiguration.class)
